# ENSAM_APNEA_CNN

Sleep apnea detection from a single-lead ECG using a 1D convolutional neural network.

Final-year engineering project (PFE) **ENSAM Rabat**. The trained model is intended for
deployment on an **ESP32-S3 N16R8** via TFLite Micro, performing one inference every 60 seconds.

---

## Overview

| | |
|---|---|
| **Dataset** | [Apnea-ECG](https://physionet.org/content/apnea-ecg/1.0.0/) (PhysioNet, Moody & Mark, 2000) |
| **Signal** | Single-lead ECG, 100 Hz, full-night recordings |
| **Input** | One 60 s window = 6000 raw samples, z-scored per window |
| **Label** | One expert annotation per minute: `A` (apnea) = 1, `N` (normal) = 0 |
| **Model** | Dilated 1D CNN 4-layer stem, 7 residual blocks (dilations 1→64) ~70 k parameters |
| **Loss** | Binary cross-entropy |
| **Decision** | Threshold selected on the validation split |
| **Framework** | TensorFlow 2.15 |

### Data split

The split is **by record, never by minute**: one patient appears in one set only, so the model
cannot learn to recognise the patient instead of the apnea. All three splits are written to disk
separately, so a consumer *cannot* accidentally train on validation records, they are not in the
file it loads.

| Split | Records | Windows | Apnea |
|---|---|---|---|
| `train` | 27 `a01–a20`, `b01–b05`, `c01–c10` minus the hold-out | 13 158 | 40.1 % |
| `val` | 8 `a05 a10 a15 a16 a17 b04 c04 c08` | 3 865 | 31.8 % |
| `test` | 35 `x01–x35`, the official PhysioNet test set | 17 248 | 38.0 % |

The 8 validation records are picked to cover all three severity groups (severe `a`, moderate `b`,
near-normal `c`), so validation mirrors the full dataset.

---

## Repository layout

```
ENSAM_APNEA_CNN/
├── data/
│   ├── apnea-ecg/                 # Raw WFDB records (.dat/.hea/.apn/.qrs), ~618 MB
│   └── processed/                 # 9 .npy arrays, ~823 MB
├── src/
│   ├── utils.py                   # Shared constants: paths, FS, record lists, SPLITS,
│   │                              #   clean_ecg(), load_record(), load_split()
│   ├── download.py                # Fetches the Apnea-ECG database from PhysioNet
│   ├── preprocess.py              # Raw WFDB records -> 3 splits x 3 .npy arrays
│   ├── model.py                   # Architecture + training
│   ├── plots.py                   # Figures
│   ├── test.py                    # Threshold choice, smoothing, scoring  (make test)
│   └── modele_CNN_commente.py     # Previous single-file pipeline, kept for reference
│
├── results/                       # model.keras, history.json, training_curves.png
├── notebooks/
├── Dockerfile
├── docker-compose.yml
├── Makefile
├── requirements.txt
└── .env
```

`data/` is git-ignored; it is regenerated by `make download` and `make prep`.


---

## Requirements

### Software

- **Docker Engine** 20.10+ with **Compose v2**
- **GNU Make**

Everything else (Python 3.11 and all libraries) lives inside the image.
Nothing needs to be installed on the host.

## Prerequisites

`.env` is required: `docker-compose.yml` declares it under `env_file` and Compose fails to start
without it. It holds the two paths the code reads from the environment:

```ini
DATA_PATH=/data          # mounted from ./data
RESULTS_PATH=/results    # mounted from ./results
```

Both are container paths mapped to host directories by the `volumes:` section, so downloaded data,
processed arrays, trained models and plots all persist on the host and survive `make down`.

---

## Quick start

Full run, from an empty clone to a scored model:

```bash
make build      # build the apnea-cnn:0.1 image
make up         # start the container in the background
make download   # fetch Apnea-ECG from PhysioNet (~618 MB, several minutes)
make prep       # preprocess raw ECG signals and build the .npy arrays (~823 MB)
make train      # train, save model + history + curves to ./results
make test       # choose the threshold on val, score the untouched x records
```

If the raw data is already in `data/apnea-ecg/`, the whole thing collapses to:

```bash
make all        # = build + up + prep + train + test
```

> `make all` intentionally does **not** include `download`. Downloading is slow and only needs to
> happen once, so it is kept as a separate explicit step.

---

## Pipeline

### 1. Download (`make download`)

Runs [src/download.py](src/download.py), which calls `wfdb.dl_database("apnea-ecg")` and writes
the 70 records into `data/apnea-ecg/` (328 files: `.dat` signals, `.hea` headers, `.apn` expert
annotations, `.qrs` beat annotations).

`overwrite=False`, so re-running it resumes rather than re-downloading.

### 2. Preprocess (`make prep`)

Runs [src/preprocess.py](src/preprocess.py). For every record:

1. `load_record()` reads the ECG channel and its per-minute `A`/`N` annotations.
2. `clean_ecg()` applies a 4th-order Butterworth band-pass (0.5–40 Hz), a 50 Hz notch (mains
   interference), NaN replacement by the median, and clipping to ±5 mV. Both filters are
   zero-phase (`filtfilt`), so R-peak positions are preserved.
3. Each labelled minute becomes one `(6000,)` row, z-scored against its own mean and standard
   deviation. This discards the patient-specific amplitude offset and keeps only the *shape* of
   the minute.
4. Windows running past the end of a recording, or carrying a symbol other than `A`/`N`, are
   skipped. Those are the only two reasons a minute is ever dropped — both structural, never
   quality-based.

Nothing is shuffled at save time: `model.py` shuffles the whole training set every epoch through
`tf.data`, and the test array is deliberately left in record order so `test.py` can smooth along it.

A `validate()` step asserts shape, label dtype, absence of NaN/Inf, apnea fraction and
normalisation **before** anything is written to disk. New invariants belong there as assertions,
not warnings.

Output: nine arrays in `data/processed/`, three per split.

| File | Shape | Size |
|---|---|---|
| `X_train_apnea.npy` | (13158, 6000) float32 | 316 MB |
| `y_train_apnea.npy` | (13158,) float32 | 53 KB |
| `counts_train_apnea.npy` | (27,) int32 | 108 B |
| `X_val_apnea.npy` | (3865, 6000) float32 | 93 MB |
| `y_val_apnea.npy` | (3865,) float32 | 15 KB |
| `counts_val_apnea.npy` | (8,) int32 | 32 B |
| `X_test_apnea.npy` | (17248, 6000) float32 | 414 MB |
| `y_test_apnea.npy` | (17248,) float32 | 69 KB |
| `counts_test_apnea.npy` | (35,) int32 | 140 B |

- **`counts` is the record axis.** It holds how many minutes each recording contributed, the only thing    
  left, after concatenation, that says where one night ends and the next begins. `test.py`
  needs it so the temporal smoothing never averages across two different sleepers.

### 3. Train (`make train`)

Runs [src/model.py](src/model.py), which loads `train` and `val`, adds the channel axis, and fits.

```
Input (6000, 1)
  stem1  Conv1D(16, 15, stride 2) → BN → ReLU                6000 → 3000
  stem2  Conv1D(24, 7)  → BN → ReLU → MaxPool(2)             3000 → 1500
  stem3  Conv1D(40, 7)  → BN → ReLU → MaxPool(2)             1500 →  750
  stem4  Conv1D(32, 5)  → BN → ReLU → MaxPool(2)              750 →  375

  7 × residual block, dilation 1, 2, 4, 8, 16, 32, 64        (375, 32)
        Conv1D(32, 3, dilated) → BN → ReLU → SpatialDropout(0.10)
        Conv1D(32, 3, dilated) → BN → squeeze-excite(ratio 8)
        + shortcut → ReLU

  concat[ attention pool | global avg pool | global max pool ]   → (96,)
  Dense(64, relu) → Dropout(0.30)
  Dense(1, sigmoid)
```

~70 k parameters. The stem strides the 6000-sample window down to 375 timesteps, then the dilated
trunk widens the receptive field to **8225 samples (82.2 s)** — `build_model()` asserts this covers
the 60 s window, so a change to `STEM` or `DILATIONS` that breaks the coverage fails loudly at
build time instead of training a model that cannot see the whole minute.

Training uses Adam (lr 1e-3), binary cross-entropy, batch size 64, up to 70 epochs, with
`EarlyStopping(monitor='val_auc', patience=12, restore_best_weights=True)` and
`ReduceLROnPlateau(monitor='val_auc', factor=0.5, patience=6)`. Seeds are fixed at 42 for
`random`, `numpy` and `tensorflow`.

Written to `results/`:

- `model.keras` — the trained model, at the epoch that maximised `val_auc`
- `history.json` — every metric at every epoch
- `training_curves.png` — four panels, drawn by [src/plots.py](src/plots.py)

The four panels each answer a different question: **loss** (is it overfitting?), **AUC** (the
monitored metric — a spiky maximum means the saved weights were a lucky epoch rather than a
converged one), **precision & recall** (both at Keras' fixed 0.5, so their distance from the
threshold `test.py` actually picks reads the sigmoid's miscalibration), and **learning rate**
(was each AUC plateau really broken by a rate cut, or are the last epochs wasted?). A dashed rule
marks the restored epoch; everything to its right was trained and discarded.

`plots.py` draws and never decides — it takes the history dict and derives nothing itself, so a
figure cannot disagree with the run behind it. That also means the curves can be redrawn from a
saved history without retraining:

```python
import json, plots
plots.plot_history(json.load(open("results/history.json")))
```

### 4. Test (`make test`)

Runs [src/test.py](src/test.py):

1. Predicts on `val` and `test`.
2. **Smooths** each probability sequence with a 5-minute moving average, *within* each recording —
   `counts` supplies the boundaries. Apnea arrives in runs of 5–30 minutes, not isolated ones, and
   the model scores each minute in complete isolation, so a single hesitant minute inside a genuine
   episode becomes a false negative even though both neighbours are confident. Set `SMOOTH_WINDOW = 1`
   to disable. On the device this costs a 5-element ring buffer and a 2-minute reporting lag.
3. **Chooses the decision threshold on `val`**, sweeping 0.05–0.95 in steps of 0.01 and maximising
   `THRESHOLD_CRITERION` (`accuracy`, `youden` or `f1`).
4. Applies that threshold to the untouched `x` records and prints AUC, per-class recall, precision,
   F1, accuracy and the confusion matrix for both splits.

The threshold is picked on validation and only then applied to test — tuning it on the test set
would report a score no deployment could reproduce.

To score a specific model: `docker compose exec apnea_cnn python test.py <name>.keras`.

The last recorded run is kept in [results/output.txt](results/output.txt). Read
[docs/MODEL_REVIEW.md](docs/MODEL_REVIEW.md) before quoting any metric — it catalogues the known
defects and flags where committed numbers disagree with each other.

---

## Make targets

| Target | Effect |
|---|---|
| `make build` | Build the `apnea-cnn:0.1` image |
| `make up` | Start the container detached |
| `make download` | Download Apnea-ECG from PhysioNet *(implies `up`)* |
| `make prep` | Build the `.npy` arrays *(implies `up`)* |
| `make train` | Train, save model + history + curves *(implies `up`)* |
| `make test` | Choose the threshold and score *(implies `up`)* |
| `make all` | `build` + `up` + `prep` + `train` + `test` |
| `make shell` | Open an interactive `bash` inside the container *(implies `up`)* |
| `make rebuild` | Rebuild the image and restart (`up -d --build`) |
| `make logs` | Follow container logs |
| `make ps` | Show container status |
| `make stop` | Stop the container, keep it |
| `make down` | Stop and remove the container |
| `make clean` | ⚠️ `down -v` **and** `docker system prune -af` (see below) |

---

## Managing the environment

**After changing `requirements.txt`.** Same: `make rebuild`.

**Freeing up.** `make down` removes the container; `data/` and `results/` are untouched.

> ⚠️ **`make clean` is aggressive.** It runs `docker system prune -af`, which deletes *all* unused
> Docker images, containers, networks and build cache on your machine, not just this project's.
> Your `data/` and `results/` directories are safe (they live on the host), but unrelated Docker
> work will be wiped. Prefer `make down` unless you specifically want a full Docker cleanup.

---
