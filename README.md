# ENSAM_APNEA_CNN

Sleep apnea detection from a single-lead ECG using a 1D convolutional neural network.

Final-year engineering project (PFE) **ENSAM Rabat**. The trained model is intended for
deployment on an **ESP32-S3 N16R8** via TFLite Micro, performing one inference every 60 seconds.

---

## Overview

| | |
|---|---|
| **Dataset** | [Apnea-ECG](https://physionet.org/content/apnea-ecg/1.0.0/) (PhysioNet, Moody & Mark, 2000) |
| **Signal** | Single-lead ECG, 100 Hz, full-night recordings |
| **Input** | One 60 s window = 6000 raw samples, z-scored per window |
| **Label** | One expert annotation per minute: `A` (apnea) = 1, `N` (normal) = 0 |
| **Model** | 1D CNN, 4 convolutional blocks + global max pooling + dense head |
| **Loss** | Custom focal loss (γ=2.0, α=0.53) to handle the 38 % / 62 % class imbalance |
| **Framework** | TensorFlow 2.15 (CPU build) |

### Data split

The split is **by record, never by minute**: one patient appears in one set only, so the model
cannot learn to recognise the patient instead of the apnea.

| Split | Records | Windows |
|---|---|---|
| Learning (`a01–a20`, `b01–b05`, `c01–c10`) | 35 | 17 023 |
| Test (`x01–x35`, official PhysioNet test set) | 35 | 17 248 |

---

## Repository layout

```
ENSAM_APNEA_CNN/
├── data/
│   ├── apnea-ecg/                 # Raw WFDB records (.dat/.hea/.apn/.qrs), ~618 MB
│   └── processed/                 # X_train_apnea.npy, y_train_apnea.npy,
│                                  #   X_test_apnea.npy, y_test_apnea.npy, ~800 MB
├── src/
│   ├── utils.py                   # Shared constants: paths, FS, record lists, splits,
│   │                              #   clean_ecg() filtering, load_record()
│   ├── download.py                # Fetches the Apnea-ECG database from PhysioNet
│   ├── preprocess.py              # Raw WFDB records -> 4 .npy arrays
│   └── modele_CNN_commente.py     # Model definition, training, evaluation, plots
│
├── results/                       # Trained .keras model + performance plots
├── notebooks/
├── Dockerfile
├── docker-compose.yml
├── Makefile
├── requirements.txt
└── .env.example
```

`data/` is git-ignored; it is regenerated by `make download` and `make prep`.

---

## Requirements

### Software

- **Docker Engine** 20.10+ with **Compose v2**
- **GNU Make**

Everything else (Python 3.11 and all libraries) lives inside the image. 
Nothing needs to be installed on the host.

## Prerequisites

`.env` is required: `docker-compose.yml` declares it under `env_file` and Compose fails to start
without it. It holds the two paths the code reads from the environment:

```ini
DATA_PATH=/data          # mounted from ./data
RESULTS_PATH=/results    # mounted from ./results
```

Both are container paths mapped to host directories by the `volumes:` section, so downloaded data, processed arrays, trained models and plots all persist on the host and survive `make down`.

---

## Quick start

Full run, from an empty clone to a trained model:

```bash
make build      # build the apnea-cnn:0.1 image
make up         # start the container in the background
make download   # fetch Apnea-ECG from PhysioNet (~618 MB, several minutes)
make prep       # preprocess raw ECG signals and build the .npy arrays (~800 MB)
make train      # train, evaluate, save model + plots to ./results
```

If the raw data is already in `data/apnea-ecg/`, the whole thing collapses to:

```bash
make all        # = build + up + prep + train
```

> `make all` intentionally does **not** include `download`. Downloading is slow and only needs to
> happen once, so it is kept as a separate explicit step.

---

## Pipeline

### 1. Download (`make download`)

Runs [src/download.py](src/download.py), which calls `wfdb.dl_database("apnea-ecg")` and writes
the 70 records into `data/apnea-ecg/` (328 files: `.dat` signals, `.hea` headers, `.apn` expert
annotations, `.qrs` beat annotations).

`overwrite=False`, so re-running it resumes rather than re-downloading.

### 2. Preprocess (`make prep`)

Runs [src/preprocess.py](src/preprocess.py). For every record:

1. `load_record()` reads the ECG channel and its per-minute `A`/`N` annotations.
2. `clean_ecg()` applies a 4th-order Butterworth band-pass (0.5–40 Hz), a 50 Hz notch (mains interference), NaN replacement by the median, and clipping to ±5 mV. Both filters are
zero-phase (`filtfilt`), so R-peak positions are preserved.
3. Each labelled minute becomes one `(6000,)` row, z-scored against its own mean and standard deviation. This discards the patient-specific amplitude offset and keeps only the *shape* of the minute.
4. Windows running past the end of a recording, or carrying a symbol other than `A`/`N`, are skipped.

The training split is shuffled once with a fixed seed (42) before saving, because Keras'
`validation_split` takes the *last* 20 % of the array as-is; unshuffled, that would be almost
entirely `c` records (nearly normal sleepers) and `val_auc` would be meaningless.

A `validate()` step asserts shape, label dtype, absence of NaN/Inf, apnea fraction and
normalisation **before** anything is written to disk.

Output: four arrays in `data/processed/`.

| File | Shape | Size |
|---|---|---|
| `X_train_apnea.npy` | (17023, 6000) float32 | 409 MB |
| `y_train_apnea.npy` | (17023,) float32 | 68 KB |
| `X_test_apnea.npy` | (17248, 6000) float32 | 414 MB |
| `y_test_apnea.npy` | (17248,) float32 | 69 KB |

Labels are stored as **float32**, not integers: the custom focal loss multiplies `y_true` by a
float tensor directly, and an integer label array would crash training with a `Mul` dtype error.

### 3. Train (`make train`)

Runs [src/modele_CNN_commente.py](src/modele_CNN_commente.py), which loads the four arrays, adds
the channel axis, builds and trains the CNN, then evaluates on the untouched `x` records.

Architecture:

```
Input (6000, 1)
  Conv1D(64, 15) → BN → MaxPool(2) → Dropout(0.3)     6000 → 3000
  Conv1D(128, 9) → BN → MaxPool(2) → Dropout(0.3)     3000 → 1500
  Conv1D(256, 5) → BN → MaxPool(2) → Dropout(0.3)     1500 →  750
  Conv1D(256, 3) → BN → MaxPool(2) → Dropout(0.3)      750 →  375
  GlobalMaxPooling1D()                                (375,256) → (256,)
  Dense(128, relu, L2=0.0015) → Dropout(0.45)
  Dense(1, sigmoid)
```

Training uses Adam (lr 8e-5), focal loss, `class_weight={0: 1.7, 1: 1.3}`, batch size 64, up to 70 epochs, with `EarlyStopping(monitor='val_auc', patience=20, restore_best_weights=True)` and
`ReduceLROnPlateau(factor=0.5, patience=7)`. Seeds are fixed at 42 for `random`, `numpy` and
`tensorflow`.

Written to `results/`:

- `model_cnn_<VERSION>.keras`: the trained model
- `courbes_<VERSION>.png`: accuracy, loss, ROC and precision-recall curves

where `VERSION` encodes the hyperparameters, e.g. `v3_4conv_a0.53_lr8e-05_cw1.7_run1`.

The script also prints a confusion matrix, a comparison against the previous reference model, and
a threshold sweep from 0.50 to 0.68 in steps of 0.02.

---

## Make targets

| Target | Effect |
|---|---|
| `make build` | Build the `apnea-cnn:0.1` image |
| `make up` | Start the container detached |
| `make download` | Download Apnea-ECG from PhysioNet *(requires `make up` first)* |
| `make prep` | Build the `.npy` arrays *(implies `up`)* |
| `make train` | Train and evaluate *(implies `up`)* |
| `make all` | `build` + `up` + `prep` + `train` |
| `make shell` | Open an interactive `bash` inside the container *(implies `up`)* |
| `make rebuild` | Rebuild the image and restart (`up -d --build`) |
| `make logs` | Follow container logs |
| `make ps` | Show container status |
| `make stop` | Stop the container, keep it |
| `make down` | Stop and remove the container |
| `make clean` | ⚠️ `down -v` **and** `docker system prune -af` (see below) |

---

## Managing the environment

**Day-to-day.** The container is long-lived (`stdin_open`/`tty` keep it alive on `bash`), and every task target runs through `docker compose exec`. So `make up` once, then `make prep` / `make train` / `make shell` as needed.

**After changing a source file.** The `src/*.py` files are `COPY`'d into the image at build time; they are *not* bind-mounted. A code change therefore requires a rebuild:

```bash
make rebuild
```

Only `./data` and `./results` are mounted live.

**After changing `requirements.txt`.** Same: `make rebuild`.

**Freeing up.** `make down` removes the container; `data/` and `results/` are untouched.

> ⚠️ **`make clean` is aggressive.** It runs `docker system prune -af`, which deletes *all* unused
> Docker images, containers, networks and build cache on your machine, not just this project's.
> Your `data/` and `results/` directories are safe (they live on the host), but unrelated Docker
> work will be wiped. Prefer `make down` unless you specifically want a full Docker cleanup.

---

## Running without Docker

Supported, but the two scripts resolve paths differently, so **the working directory matters**:

```bash
python3.11 -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt

# download + preprocess: run from the project ROOT
#   (utils.py defaults DATA_PATH to "data", relative to the cwd)
python src/download.py     # ⚠ see note below
python src/preprocess.py

# training: run from src/
#   (paths '../data/processed' and '../results' are hardcoded)
cd src && python modele_CNN_commente.py
```

---
